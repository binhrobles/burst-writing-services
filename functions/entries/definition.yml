---
entries:
  handler: functions/entries/handler.create
  role: BasicRole
  events:
    - http:
        method: post
        path: entry
        request:
          schema:
            application/json: ${file(functions/entries/models/EntryCreate.json)}
